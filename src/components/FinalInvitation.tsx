import { MapPin } from 'lucide-react';import { photos } from '../data/photos';import { event } from '../config/event';import { Reveal } from './Reveal';import { ShareButton } from './ShareButton';import { WhatsAppRSVP } from './WhatsAppRSVP'
export function FinalInvitation(){return <footer className="final"><img src={photos.closing.src} alt={photos.closing.alt} width={photos.closing.width} height={photos.closing.height} loading="lazy"/><div className="final-wash"/><Reveal className="final-copy"><p>We would be blessed<br/>to celebrate this beautiful day with you.</p><h2>{event.childName}</h2><span className="final-date">30 · 08 · 2026</span><span className="cross">✝</span><strong>See you there. ♡</strong><div className="final-actions"><a className="button button-cream" href={event.mapUrl} target="_blank" rel="noopener noreferrer"><MapPin/>Get Directions</a><ShareButton/><WhatsAppRSVP/></div></Reveal></footer>}
