import { useState } from 'react';import { Share2 } from 'lucide-react'
export function ShareButton(){const [copied,setCopied]=useState(false);async function share(){const data={title:'Jake John Jobin | Holy Baptism',text:"Join us in celebrating Jake's Holy Baptism on 30 August 2026.",url:location.href};try{if(navigator.share)await navigator.share(data);else{await navigator.clipboard.writeText(location.href);setCopied(true);setTimeout(()=>setCopied(false),2500)}}catch{/* Share sheet dismissed. */}}return <div className="share-wrap"><button className="button button-light" onClick={share}><Share2/>Share Invitation</button><span role="status">{copied?'Invitation link copied':''}</span></div>}
