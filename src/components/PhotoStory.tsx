import { photos } from '../data/photos'
import { Reveal } from './Reveal'
export function PhotoStory(){const featured=[photos.story[0],photos.father];return <section className="story"><Reveal className="story-heading"><span className="cross">✝</span><p>A little miracle.<br/><em>A lifetime of love.</em></p></Reveal><div className="story-grid story-grid-quiet">{featured.map((p,i)=><Reveal className={`story-tile story-tile-${i}`} key={p.src} delay={i*.08}><img src={p.src} alt={p.alt} width={p.width} height={p.height} loading="lazy"/></Reveal>)}</div><Reveal className="story-closing"><p className="eyebrow">Blessed beyond measure</p></Reveal></section>}
